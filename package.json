{
  "name": "help-teacher",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build -w packages/shared && concurrently -n backend,frontend -c blue,green \"npm run start:dev -w backend\" \"npm run start -w frontend\"",
    "dev:backend": "npm run start:dev -w backend",
    "dev:frontend": "npm run start -w frontend",
    "build:shared": "npm run build -w packages/shared",
    "db:up": "cd backend && npx supabase start",
    "db:down": "cd backend && npx supabase stop --no-backup"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
