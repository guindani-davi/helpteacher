name: Deploy Migrations to Preview Database
on:
  push:
    branches-ignore:
      - main
  workflow_dispatch:
jobs:
  deploy-migrations:
    runs-on: ubuntu-latest
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_DB_PASSWORD: ${{ secrets.PREVIEW_DB_PASSWORD }}
      SUPABASE_PROJECT_ID: ${{ secrets.PREVIEW_PROJECT_ID }}
    steps:
      - uses: actions/checkout@v4
      - uses: supabase/setup-cli@v1
        with:
          version: latest
      - run: cd backend && supabase link --project-ref $SUPABASE_PROJECT_ID
      - run: cd backend && supabase db push
